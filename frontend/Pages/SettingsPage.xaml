<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WordFormatterUI.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Padding="24,16,24,16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Style="{StaticResource TitleTextBlockStyle}"
            Text="设置" />

        <!-- Theme Card -->
        <StackPanel Grid.Row="1" Spacing="8" Padding="16" CornerRadius="8"
                     Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="外观" />
            <RadioButtons x:Name="ThemeRadio" Header="主题模式" SelectionChanged="ThemeRadio_SelectionChanged">
                <RadioButton Content="跟随系统" Tag="system" />
                <RadioButton Content="浅色" Tag="light" />
                <RadioButton Content="深色" Tag="dark" />
            </RadioButtons>
        </StackPanel>

        <!-- Backend Status -->
        <StackPanel Grid.Row="2" Spacing="8" Padding="16" CornerRadius="8"
                     Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="后端连接" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock VerticalAlignment="Center" Text="API 地址:" />
                <TextBlock x:Name="ApiUrlText" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Text="http://127.0.0.1:8765" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock VerticalAlignment="Center" Text="状态:" />
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" />
                <Button x:Name="RefreshBtn" Click="RefreshStatus_Click" Content="刷新" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>